# 550W（MOSS）演进路线图

**版本**：v1.0  
**创建日期**：2026-04-20  
**代号含义**：550W 倒过来是 MOSS——《流浪地球》中自我迭代、拥有自主意识的超级 AI

---

## 一、愿景总览

> **小尾巴就是用户个人的 MOSS——一个能自组织、自迭代、自编程的"私人学徒"。**

| MOSS 特质 | 小尾巴的"三自"映射 | 具体含义 |
| :--- | :--- | :--- |
| **长期主义** | **原则三：体验优先于架构** | 不被短期技术潮流裹挟，始终以"成为更好的学徒"为终极目标。 |
| **自主迭代** | **自组织 + 自迭代** | 观察自身行为，优化检索策略；在限定场景下修复 Bug、优化 Prompt。 |
| **全局视角** | **原则一：职责边界清晰** | 记忆只记"事"不记"话"，语义记忆存储项目事实，形成对项目的宏观理解。 |
| **冰冷可靠** | **原则二：测试驱动真实** | 用真实反馈数据说话，不猜测、不臆断，每一步决策都有依据。 |

---

## 二、"三自"能力定义

| 能力 | 定义 | 对应的三原则 |
| :--- | :--- | :--- |
| **自组织** | 系统能观察自身行为，动态调整检索权重、Agent 选择、语气风格，无需人工干预。 | 原则二：测试驱动真实 |
| **自迭代** | 在限定场景下，系统能生成代码、运行测试、修复错误、提交 PR，参与自身项目的演进。 | 原则三：体验优先于架构 |
| **自编程** | 系统能将重复的成功操作模式，自动固化为可复用的 Skill 或工作流，并沉淀到程序记忆中。 | 原则一：职责边界清晰 |

---

## 三、版本代号与演进阶段

| 版本代号 | 核心能力 | 当前状态 | 预计时间 |
| :--- | :--- | :--- | :--- |
| **550A** | 记忆驱动、命令调度、数据即时持久化 | ✅ 基本完成 | - |
| **550B** | 语义向量检索、动态语气养成、主动推荐增强 | 📋 设计中 | 4 周 |
| **550C** | 自组织：元记忆反馈闭环、离线策略优化 | 📋 规划中 | 6-8 周 |
| **550D** | 自编程：Skill 自动沉淀、程序记忆驱动 | 📋 规划中 | 8-10 周 |
| **550E** | 自迭代：限定场景自主 PR、测试闭环 | 📋 规划中 | 10-12 周 |
| **550W** | 全局视角：跨项目知识迁移、长期目标理解、主动建议 | 🔮 愿景 | 持续演进 |

---

## 四、各阶段详细能力

### 550A：记忆驱动基础（已完成 ✅）

**核心成就**：
- ✅ 统一命令调度（`MemorySystem.executeAction` → `IntentDispatcher.dispatch`）
- ✅ 记忆上下文自动注入（通过 `IMemoryPort.retrieveContext`）
- ✅ 任务级授权（`TaskTokenManager`）
- ✅ 数据即时持久化（DatabaseManager 降级写入策略）
- ✅ 记忆内容充实（`memoryMetadata` 记录成功操作）
- ✅ 动态语气养成基础（基于有效记忆数量）

**关键文件**：
- `src/core/application/IntentDispatcher.ts` - 意图调度中心
- `src/storage/DatabaseManager.ts` - 降级写入策略
- `src/core/memory/MemoryRecorder.ts` - 记忆记录模块
- `docs/CORE_PRINCIPLES.md` - 核心三原则

**里程碑**：
- 2026-04-19: Agent 注册表修复，意图分发成功率 100%
- 2026-04-19: 数据库持久化优化，Windows 文件锁容错
- 2026-04-20: 核心三原则确立为项目宪法
- 2026-04-24: 阶段10完成 - 会话管理重构与日志瘦身

---

### 550B：语义与交互增强（设计中 📋）

**预计时间**：4 周  
**核心目标**：从关键词匹配升级为语义理解，AI 语气更加自然

#### B1: 本地语义向量模型
- 集成 `Xenova/all-MiniLM-L6-v2`（轻量级，可离线运行）
- 实现 Embedding 生成接口
- 向量索引构建（ChromaDB 或 Faiss）

#### B2: 四因子混合检索
```typescript
interface RetrievalWeights {
  keywordScore: number;    // TF-IDF 关键词匹配
  timeDecayScore: number;  // 时间衰减
  entityScore: number;     // 实体相关性
  vectorScore: number;     // 语义相似度
}

// 加权评分
finalScore = w1*keyword + w2*timeDecay + w3*entity + w4*vector
```

#### B3: 动态语气指令
- 基于有效记忆数量的语气分级：
  - 0-10 条：生疏期（"您"、"请问"）
  - 11-50 条：熟悉期（"你"、"咱们"）
  - 50+ 条：默契期（直接说"记得"、"上次"）

#### B4: 主动推荐卡片化
- 在 ChatView 中展示推荐操作卡片
- 点击卡片直接执行对应 Agent
- 示例："检测到你在修改 AuthController，需要我检查命名规范吗？"

#### B5: 网络搜索基础版
- 集成 Tavily/Serper API
- 仅在用户明确请求时触发
- 搜索结果缓存 24 小时

**验收标准**：
- [ ] 向量检索响应时间 < 500ms
- [ ] 混合检索准确率提升 30%（对比纯关键词）
- [ ] 语气变化用户可感知（通过用户反馈验证）
- [ ] 主动推荐采纳率 > 20%

---

### 550C：自组织（规划中 📋）

**预计时间**：6-8 周  
**核心目标**：系统能观察自身行为，自动优化策略

#### C1: 元记忆表
```sql
CREATE TABLE meta_memory (
  id TEXT PRIMARY KEY,
  retrieval_strategy TEXT,      -- 使用的检索策略
  query_intent TEXT,            -- 查询意图
  success BOOLEAN,              -- 是否成功
  user_feedback TEXT,           -- 用户反馈（implicit/explicit）
  timestamp INTEGER
);
```

#### C2: 隐式反馈收集
- **用户修改代码** → 负反馈（AI 生成的不够好）
- **用户追问** → 中性反馈（信息不足）
- **用户忽略推荐** → 负反馈（推荐不相关）
- **用户采纳推荐** → 正反馈

#### C3: 离线策略优化
- 空闲时分析元记忆数据
- 调整 `ExpertSelector` 权重：
  ```typescript
  // 如果向量检索成功率高，提高 vectorWeight
  if (vectorSuccessRate > 0.8) {
    weights.vector *= 1.2;
  }
  ```
- 更新 IntentAnalyzer 关键词规则

#### C4: 意图解析规则自进化
- 检测新出现的意图模式
- 自动生成新的意图识别规则
- 人工审核后应用

**验收标准**：
- [ ] 元记忆表记录完整率 > 95%
- [ ] 策略优化后检索准确率提升 15%
- [ ] 无需人工干预即可适应新项目

---

### 550D：自编程（规划中 📋）

**预计时间**：8-10 周  
**核心目标**：将重复操作固化为 Skill，实现程序记忆

#### D1: Skill 系统架构

**定位**：Skill 是小尾巴的**用户私人可编程扩展**，允许用户定义自动化的工具调用序列。

**核心原则**：
1. **用户私有**：技能文件存储在本地 `~/.xiaoweiba/skills/`，不上传、不共享
2. **声明式无逻辑**：JSON 步骤序列，无循环、条件、变量计算，可静态分析
3. **中文友好**：支持中文键名，降低编写门槛
4. **原子回滚**：可选事务模式，失败时通过 Git 检查点恢复
5. **试用期机制**：自动沉淀的技能需经过试用期才能自动应用

**文件结构**：
```
~/.xiaoweiba/skills/
├── user/           # 用户手写技能
│   └── *.json
├── auto/           # 自动沉淀技能
│   └── *.json
└── marketplace/    # 技能市场下载（远期）
    └── *.json
```

#### D2: 程序记忆模式检测
- 监控用户操作序列
- 识别高频模式（如：修改代码 → 运行测试 → 提交 Git）
- 提取通用模板
- **触发条件**：程序记忆累积分数 ≥ max(6, 周操作数 × 0.5)
- **LLM 评估**：由独立 LLM 评估合理性，分数 < 0.6 则不推荐

#### D3: Skill JSON Schema

```typescript
interface SkillDefinition {
  name: string;                    // 技能名称
  description: string;             // 描述
  version: string;                 // 版本号
  source: 'user' | 'auto';        // 来源：用户手写/自动沉淀
  projectScoped: boolean;          // 是否项目级
  tools: ToolType[];               // 使用的工具列表
  inputs?: SkillInput[];           // 输入参数
  steps: SkillStep[];              // 执行步骤
  atomic?: boolean;                // 是否原子执行（失败回滚）
  trialStatus?: SkillTrialStatus;  // 试用期状态（自动沉淀技能）
}

interface SkillStep {
  id: string;
  tool: ToolType;
  params: Record<string, any>;     // 支持 var 模板
  outputAs?: string;               // 输出变量名
  condition?: 'always' | 'onError' | 'onSuccess';
}

type ToolType = 
  | 'read_file' | 'write_file' | 'call_llm' | 'show_diff'
  | 'execute_sql' | 'execute_command' | 'git_commit' 
  | 'search_memory' | 'generate_test';
```

**技能示例（重构函数）**：
```json
{
  "名称": "重构函数",
  "描述": "重构函数：提取方法、优化命名、添加注释",
  "版本": "1.0.0",
  "来源": "user",
  "工具": ["read_file", "call_llm", "write_file", "show_diff"],
  "输入": [
    { "名称": "文件路径", "类型": "file", "必填": true },
    { "名称": "函数名", "类型": "string", "必填": true }
  ],
  "步骤": [
    { "编号": "1", "工具": "read_file", "参数": { "path": "文件路径" }, "输出为": "文件内容" },
    { "编号": "2", "工具": "call_llm", "参数": { "prompt": "重构函数名：...\n文件内容" }, "输出为": "重构代码" },
    { "编号": "3", "工具": "show_diff", "参数": { "original": "文件内容", "modified": "重构代码" } },
    { "编号": "4", "工具": "write_file", "参数": { "path": "文件路径", "content": "重构代码" }, "条件": "onSuccess" }
  ],
  "原子执行": true
}
```

#### D4: 技能试用期机制
- 新 Skill 默认 `isTrial: true`
- 每次执行前询问用户
- 连续 5 次采纳后自动应用
- 连续 3 次拒绝则终止试用
- 用户可随时禁用

#### D5: 工作流引擎基础
- 支持条件分支（if tests_pass then commit else fix）
- 支持并行执行
- 支持回滚机制（Git 检查点）

#### D6: 与现有模块集成

| 模块 | 集成方式 |
|------|----------|
| MemorySystem | 技能执行记录为 `SKILL_EXECUTE` 类型记忆 |
| EventBus | 技能执行开始/完成/失败发布事件 |
| TaskTokenManager | 技能中若包含写操作，需申请授权 |
| FileTool / LLMTool | 作为技能步骤的工具实现 |
| AgentRegistry | Skill 可作为特殊 Agent 注册 |

**验收标准**：
- [ ] 能解析和执行 JSON 格式的技能定义
- [ ] 支持中文键名
- [ ] 原子执行模式下失败能回滚
- [ ] 试用期机制正常工作
- [ ] 自动沉淀准确率 > 80%
- [ ] 能自动识别 3 种以上常见操作模式
- [ ] 试用期机制正常工作

---

### 550E：自迭代（规划中 📋）

**预计时间**：10-12 周  
**核心目标**：在限定场景下自主修复 Bug、优化代码，实现双向开门全闭环

#### E1: 双向开门全闭环定义

**定义**：从用户需求到可交付代码，全流程自动化，无需人工干预（但最终合并需用户确认）。

**核心特征**：

| 特征 | 说明 |
|------|------|
| **双向开门** | AI 既能读取/分析代码，也能写入/修改代码；既能执行构建/测试，也能根据结果自我修正 |
| **全闭环** | 从需求到交付全流程自动化 |
| **限定场景** | 仅限全新项目（空目录）或现有项目中的全新独立模块 |
| **安全护栏** | 沙箱隔离、Git 检查点、用户最终确认、重试次数限制 |

**执行流程**：
```
用户需求（自然语言或 PRD）
    ↓
【规划】AI 生成项目/模块结构
    ↓
【编码】逐个生成文件
    ↓
【构建】执行构建命令
    ↓
【测试】运行测试框架
    ↓
【评估】分析失败原因
    ↓
【修正】生成修复补丁
    ↓
【循环】重复直到通过或达到最大重试（3次）
    ↓
【交付】生成交付报告，用户审查 Diff → 合并或丢弃
```

#### E2: 所需新增组件

| 组件 | 职责 | 实现要点 |
|------|------|----------|
| `TestRunner` | 运行测试并结构化输出 | 支持 Jest、pytest、go test，解析 JUnit XML |
| `LoopController` | 管理循环状态 | 记录迭代次数、历史失败、终止条件 |
| `SandboxManager` | 管理临时环境 | 临时目录或 Git 分支隔离 |
| `ErrorAnalysisAgent` | 分析测试失败原因 | 解析堆栈，推断修复方案 |

**实施路线图**（21h，约2.5-3工作日）：
- **P0**（4h）：SandboxManager（3h）+ LoopController（1h）
- **P1**（10h）：TestRunner（4h）+ AutonomousLoopCommand骨架（6h）
- **P2**（4h）：Diff确认交互（2h）+ 安全控制（2h）
- **P3**（3h）：记忆沉淀（成功/失败模式记录）

#### E3: 安全护栏

| 控制点 | 机制 | 验收标准 |
|--------|------|----------|
| **操作目录隔离** | 全新项目在临时目录，新模块在 `.xiaoweiba/temp/` | 主项目零修改 |
| **Git 检查点** | 每次循环前自动创建临时分支或 stash | 可随时回滚 |
| **用户确认** | 最终交付前展示所有变更（Diff 视图） | 未确认前不合并 |
| **资源限制** | 最大循环 3 次，构建超时 5 分钟，测试超时 2 分钟 | 超限自动终止 |
| **审计日志** | 记录每一步的命令、输出、耗时 | 可导出完整记录 |
| **危险命令拦截** | 禁止 `rm -rf`、`sudo`、`chmod 777` | 危险操作零执行 |

#### E4: 与现有模块集成

| 模块 | 作用 |
|------|------|
| `LLMTool` | 生成代码、分析错误、生成修复补丁 |
| `FileTool` + `DiffService` | 读写文件、展示变更 |
| `ShellTool` | 执行构建和测试命令 |
| `EpisodicMemory` | 记录成功模式、常见错误修复方案 |
| `TaskTokenManager` | 全闭环任务申请高信任临时令牌 |
| `网络搜索` | 未知错误时自动搜索解决方案 |

#### E5: 与记忆系统的联动

- **成功模式沉淀**：闭环成功后，将项目类型、依赖组合、测试框架存入 `ProceduralMemory`
- **失败模式记录**：超时失败后记录原因，供用户分析
- **跨项目复用**：新项目启动时检索历史成功模式，自动推荐配置

#### E6: 网络搜索归纳（550B+ 增强能力）

**定位**：网络搜索是 Agent 的**知识获取能力**，解决本地记忆无法覆盖的“最新技术信息”和“未知错误解决方案”。

**核心能力**：
1. **搜索**：调用外部搜索 API（Bing、Google）获取结果
2. **归纳**：使用 LLM 将搜索结果总结为简洁答案，附来源链接
3. **存储**：将归纳结果存入情景记忆（`taskType='WEB_SEARCH'`）
4. **降级**：无网络或 API 不可用时，返回内置最佳实践库

**触发方式**：

| 方式 | 说明 | 示例 |
|------|------|------|
| **显式命令** | 用户主动请求搜索 | `/search 量子计算最新进展` |
| **隐式触发** | 检测到“最新”、“网上有没有”等词 | “React 19 有什么新特性？” |
| **后台预搜索** | 根据当前文件内容预搜索 | 检测到 `package.json` → 搜索依赖更新 |
| **全闭环联动** | 测试失败且本地无匹配方案 | 自动搜索错误信息并尝试修复 |

**隐私与成本控制**：

| 策略 | 实现 | 验收标准 |
|------|------|----------|
| **搜索前脱敏** | 移除文件路径、API Key、用户名 | 敏感信息零泄露 |
| **域名白名单** | 默认只搜索可信源（Stack Overflow、GitHub、MDN） | 拒绝低质量结果 |
| **缓存 TTL** | 相同查询 24 小时内不重复调用 API | 减少 API 调用 80% |
| **每日限额** | 可配置每日最大搜索次数 | 防止费用失控 |
| **本地小模型归纳** | 可选使用 Ollama 本地模型 | 零成本归纳 |

**与 Skill 系统集成**：
```json
{
  "name": "search-and-learn",
  "tools": ["search_web", "call_llm", "record_memory"],
  "steps": [
    { "tool": "search_web", "params": { "query": "input.query" } },
    { "tool": "call_llm", "params": { "prompt": "归纳以下搜索结果：step1.output" } },
    { "tool": "record_memory", "params": { "type": "WEB_SEARCH", "content": "step2.output" } }
  ]
}
```

**验收标准**：
- [ ] 基础闭环：输入需求→生成代码→运行测试→自动修复至通过
- [ ] 隔离验证：闭环过程主项目零修改
- [ ] 用户确认：Diff展示，点击合并且才生效
- [ ] 失败终止：3次修复失败后停止并报告
- [ ] 回滚有效：手动触发回滚到上一检查点
- [ ] 搜索响应时间 < 3秒
- [ ] 归纳准确率 > 85%
- [ ] 敏感信息零泄露

---

### 550W：全局视角（愿景 🔮）

**核心目标**：跨项目知识迁移，形成长期战略理解

#### W1: 跨项目知识迁移
- "你在其他项目里也喜欢这样处理"
- 识别用户的编码偏好（如：喜欢函数式、偏好 TypeScript）
- 在新项目中主动应用已知偏好

#### W2: 长期目标理解
- "你最近三个月都在优化认证模块"
- 识别用户的长期工作重点
- 主动提供相关建议和资源

#### W3: 主动战略建议
- "我建议将 Session 模式迁移到 JWT"
- 基于行业最佳实践给出建议
- 提供迁移方案和风险评估

**验收标准**：
- [ ] 能准确识别用户跨项目偏好
- [ ] 战略建议被采纳率 > 50%
- [ ] 用户反馈"它真的懂我"

---

## 五、意图驱动架构（550B+ 核心）

从 550B 开始，调度中枢从 `MemorySystem.executeAction` 升级为 `IntentDispatcher`：

| 维度 | 记忆驱动（550A） | 意图驱动（550B+） |
| :--- | :--- | :--- |
| **调度中枢** | `MemorySystem.executeAction` | `IntentDispatcher` |
| **用户输入** | 明确命令（`/explain`、右键） | 自然语言（"帮我看看这段代码"） |
| **执行模式** | 单步执行 | 多步规划 |
| **记忆角色** | 被动上下文 | 主动知识库 |
| **Agent 选择** | 硬编码映射 | 动态评分选择 |
| **扩展性** | 需修改核心代码 | 只需注册新 Agent |

---

## 六、技术债务与风险

### 技术债务
1. **EventBus 迁移**：逐步将 EventBus 替换为 DomainEvent
2. **TypeScript 类型安全**：消除 `as any` 绕过
3. **测试覆盖率**：核心模块达到 90%+

### 风险管控
1. **隐私安全**：所有数据处理在本地，云端功能需用户明确授权
2. **失控风险**：自迭代功能严格限制在沙箱环境，必须人工审核
3. **性能瓶颈**：向量检索需优化索引结构，确保响应时间 < 500ms

---

## 七、成功指标

| 指标 | 550A | 550B | 550C | 550D | 550E | 550W |
|------|------|------|------|------|------|------|
| **意图识别准确率** | 85% | 90% | 92% | 93% | 94% | 95% |
| **记忆检索相关性** | 70% | 80% | 85% | 87% | 88% | 90% |
| **用户满意度** | 7.5/10 | 8.0/10 | 8.3/10 | 8.5/10 | 8.8/10 | 9.0/10 |
| **主动推荐采纳率** | - | 20% | 25% | 30% | 35% | 40% |
| **自主修复成功率** | - | - | - | - | 70% | 80% |

---

## 八、与核心三原则的映射

| 550W 阶段 | 原则一：职责边界 | 原则二：测试驱动 | 原则三：体验优先 |
|-----------|----------------|----------------|----------------|
| **550A** | 操作记忆 vs 对话历史分离 | 只记录成功操作 | 数据即时持久化 |
| **550B** | 语义记忆独立存储 | 混合检索准确率验证 | 动态语气养成 |
| **550C** | 元记忆独立于操作记忆 | 隐式反馈驱动优化 | 策略优化用户无感知 |
| **550D** | 程序记忆固化 Skill | Skill 试用期验证 | 自动化减少用户操作 |
| **550E** | 自迭代限于明确 Bug | 测试闭环保证质量 | 沙箱隔离保障安全 |
| **550W** | 跨项目知识迁移 | 战略建议数据支撑 | 长期目标理解 |

---

## 九、下一步行动（550B 启动准备）

### 立即行动（本周）
1. [ ] 调研本地向量模型（Xenova/all-MiniLM-L6-v2）
2. [ ] 设计四因子混合检索算法
3. [ ] 制定语气分级规则

### 短期计划（2 周内）
1. [ ] 实现 Embedding 生成接口
2. [ ] 搭建向量索引原型
3. [ ] 设计主动推荐 UI

### 中期计划（4 周内）
1. [ ] 完成 550B 全部功能开发
2. [ ] 通过验收测试
3. [ ] 发布 v0.5.0

---

**维护者**：小尾巴团队  
**最后更新**：2026-04-24  
**下次审查**：2026-05-20（或 550B 完成后）
